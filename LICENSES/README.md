# Acquired license texts

`eng/Acquire-FFmpegRuntime.ps1` copies the BtbN archive license here as
`FFmpeg-LGPL-3.0.txt`. Git ignores it together with the native DLLs.
`eng/Pack.ps1` requires the file; the nuspec includes it in the package.
